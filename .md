/sp.specify Backend development for Todo Application 

Project context: `./server/CLAUDE.md`
Subagent : `backend-developer`
Code location: `./server` (project already initialized, dependencies installed)

Focus:
- RESTful backend for a todo application using FastAPI
- Authentication compatible with BetterAuth
- Persistent storage using PostgreSQL with SQLModel
- Task management features defined in Phase 2

Success criteria:
- All task-related routes implemented and functional
- Authentication routes compatible with BetterAuth flows
- SQLModel schemas correctly defined and migrated
- API is runnable locally with existing setup
- Endpoints follow clean, maintainable structure

Backend scope:

1. Authentication (FastAPI + BetterAuth compatible)
   - User registration endpoint
   - User login endpoint
   - Token-based authentication (access token handling)
   - Auth dependency/middleware for protected routes
   - Current-user resolution from token

2. Task management routes (Phase 2)
   - Create task
   - Get all tasks for authenticated user
   - Get single task by ID
   - Update task (title, description, status)
   - Mark task as completed / incomplete
   - Delete task
   - Ensure all task routes are protected and user-scoped

3. Database & models (SQLModel)
   - User model (id, email/username, hashed password, timestamps)
   - Task model (id, title, description, completed/status, user_id, timestamps)
   - Proper relationships between User and Task
   - Pydantic/SQLModel schemas for:
     - Create
     - Read
     - Update
   - Database session management

4. Application structure
   - Modular routers (auth, tasks)
   - Clear separation of:
     - models
     - schemas
     - routers
     - dependencies
   - Main FastAPI app configuration
   - Include routers and auth dependencies correctly

Constraints:
- Use FastAPI and SQLModel only (no ORM alternatives)
- PostgreSQL as the database
- No frontend code
- No tests required in this phase
- Do not reinstall or modify dependencies
- Assume environment variables and database config already exist

Not building:
- Frontend integration
- Background jobs or queues
- Role-based access control beyond basic user ownership
- Advanced filtering, pagination, or search
- Deployment configuration


check out how speckit plus /sp.specify command is looking like for a research paper 

```markdown
/sp.specify Research paper on AI's impact on K-12 classroom efficiency

Target audience: Education administrators evaluating AI adoption
Focus: Teacher workload reduction and student outcome improvements

Success criteria:
- Identifies 3+ concrete AI applications with evidence
- Cites 8+ peer-reviewed academic sources
- Reader can explain ROI of classroom AI after reading
- All claims supported by evidence

Constraints:
- Word count: 3000-5000 words
- Format: Markdown source, APA citations
- Sources: Peer-reviewed journals, published within past 10 years
- Timeline: Complete within 2 weeks

Not building:
- Comprehensive literature review of entire AI field
- Comparison of specific AI products/vendors
- Discussion of ethical concerns (separate paper)
- Implementation guide or code examples
```

Generate a `/sp.specify` command for me for the development of backend of todo application in phase 2 (refer hackathon docs) which tell claude code cli to write backend code in a project folder `./server` where all the project is already setup with all dependencies installed .

- Provide me markdown command only
- Mention all the important parts of backend , including all the tasks related routes (mentioned for phase2 in page no 7) , better auth compatible fastapi routes for authentication , sqlmodel schema on page no 14   



/sp.plan   ## Create: functional specification, module-level design, CLI flow outline, and implementation checklist.



**Decisions needing documentation**: task ID strategy, class responsibilities, module boundaries, error-handling patterns.
**Testing strategy**: manual CLI testing against success criteria and feature checklist.

## Technical details:
- Follow top-down approach (define behavior before implementation)
- Organize by layers: CLI → Domain Models → In-Memory Store
- Implement incrementally by feature: Add → View → Update → Delete → Toggle Complete
- Validate quality against Constitution (OOP, simplicity, determinism, readability)

## **Note :**
Must use context 7 MCP server read the following docs before :
- [Questionary](https://github.com/tmbo/questionary)
- [UV] (https://docs.astral.sh/uv/)